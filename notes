To make a request in JS use the code from front end lioness component.
To make a request in Python use this code. 

import requests

test_url = "http://url/api"

test_files = {
    "test_file_1": open("my_file.txt", "rb"),
    "test_file_2": open("my_file_2.txt", "rb"),
    "test_file_3": open("my_file_3.txt", "rb")
}

arguments = {'alpha' : 0.1,
		'precision': 0,
		'mode' : union,
		'start' : 0,
		'end' : 0
		}
test_response = requests.post(test_url, files = test_files, data = arguments)

if test_response.ok:
    print("Upload completed successfully!")
    print(test_response.text)
else:
    print("Something went wrong!")

